function [l, P] = predict(l,P)
    % this function is the prediction step of the kalman filter
    % - l = [x,y]: landmark position - previous state estimation
    % - P: covariance of the previous state estimation
    
    l = l;
    P = P;
end